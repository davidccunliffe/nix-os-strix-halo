# Template for /var/lib/llama/env (root:llama, mode 0600).
# Install with:
#   echo 'LLAMA_API_KEY=...' | sudo install -m 0600 -o llama /dev/stdin /var/lib/llama/env
# Invent one long random value, e.g.: openssl rand -hex 32
LLAMA_API_KEY=change-me-long-random
