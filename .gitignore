# macOS
.DS_Store

# nix build outputs
result
result-*

# direnv
.direnv/

# rendered artifacts (regenerate from the .md)
*.pdf

# real secrets never live in this repo (they belong in /var/lib/*/env);
# only the *.env.example templates are tracked
secrets/*.env

# Claude Code per-machine permissions; install.sh does `git add -A`,
# so this needs ignoring or every install stages it
.claude/settings.local.json
